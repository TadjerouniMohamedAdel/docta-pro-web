import React from 'react';
import './styles.less';

const Logo = () => {
  return (
    <div
      className="logo-wrapper"
      style={{
        display: 'flex',
        alignItems: 'center',
        justifyContent: 'center',
      }}
    >
      Docta
    </div>
  );
};

export default Logo;

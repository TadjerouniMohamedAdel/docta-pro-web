import React from 'react';

const Header = () => {
  return (
    <header
      className="header"
      style={{ display: 'flex', alignItems: 'center', padding: 20 }}
    >
      Header
    </header>
  );
};

export default Header;
